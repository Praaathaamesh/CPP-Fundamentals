#include <iostream>
#include <cmath>

int main(){

    double x = 3.14;
    double y = 10;
    double z;

    //z = std::max(x, y);
    //z = std::min(x, y);
    //z = abs(x);
    //z = floor(x);
    //z = ceil(x);
    //z = pow(2, 3);
    z = sqrt (9);
    // more at www.cplusplus.com/reference/cmath/
    std::cout << z << '\n';

    return 0;
}